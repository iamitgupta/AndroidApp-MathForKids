<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.amit.mathforkids">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/test"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/test"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".add" />
        <activity android:name=".div" />
        <activity android:name=".mul" />
        <activity android:name=".sub" />
        <activity android:name=".addtest" />
        <activity android:name=".subtest" />
        <activity android:name=".multest" />
        <activity android:name=".divtest" />
        <activity android:name=".help"></activity>
    </application>

</manifest>